.col-sm-5
  %p.in-gold-500.font-18.mb-25= t('settings.email')
  = form_for(current_user, method: :put) do |f|
    .form-group.mb-35
      = f.label :email, t('auth.enter_email'), class: 'control-label input-label'
      = f.email_field(:email,
                      autofocus: true,
                      autocomplete: 'email',
                      id: 'email',
                      class: 'form-control',
                      required: true,
                      placeholder: t('auth.enter_email'))
    .general-text-align
      = f.submit t('settings.save'), class: 'btn btn-default mb-35', id: 'saveEmail'

.col-sm-5.col-sm-offset-1
  %p.in-gold-500.font-18.mb-25= t('auth.password')
  = form_for(current_user, method: :put) do |f|
    .form-group.mb-30
      = f.label :current_password, t('settings.old_password'), class: 'control-label input-label'
      = f.password_field(:current_password,
                        autocomplete: 'current_password',
                        id: 'current_password',
                        class: 'form-control',
                        required: true,
                        placeholder: t('settings.old_password'))

    .form-group.mb-30
      = f.label :password, t('auth.type_new_password'), class: 'control-label input-label'
      = f.password_field(:password,
                        autocomplete: 'password',
                        id: 'password',
                        class: 'form-control',
                        required: true,
                        placeholder: t('auth.type_new_password'))

    .form-group.mb-40
      = f.label :password_confirmation, t('auth.confirm_password'), class: 'control-label input-label'
      = f.password_field(:password_confirmation,
                      autocomplete: 'password_confirmation',
                      id: 'password_confirmation',
                      class: 'form-control',
                      required: true,
                      placeholder: t('auth.confirm_password'))

    .general-text-align
      = f.submit t('settings.save'), class: 'btn btn-default mb-10', id: 'savePassword'

.col-sm-12
  %p.in-gold-500.font-18.mb-25= t('settings.remove')
  = form_for(current_user, method: :delete) do |f|
    .general-settings-btn
      = f.submit t('settings.please_remove'), class: 'btn mb-20', id: 'delete', disabled: true

    .form-group.checkbox.general-settings-checkbox{ onclick: "account_destroy();" }
      %label.checkbox-label
        %input.checkbox-input{type: "checkbox", hidden: "true", id: 'agree'}
        %span.checkbox-icon
          %i.fa.fa-check
        %span.checkbox-text= t('settings.understand')
